# 📝 2026-09-15 (화) 실습 정리: Vue 3 + Vuetify 3 + Axios 기반 Todolist & Slot/i18n 실습

본 문서는 `todolist` 프로젝트의 전체 코드 구조, 컴포넌트 설계, 슬롯(Slot) 메커니즘, Axios 비동기 통신, Vuetify 3 UI 컴포넌트 및 TypeScript 연동 원리를 상세히 기록한 실습 아카이브 문서입니다.

---

## 📑 목차
1. [실행 환경 및 진입 구조](#1-실행-환경-및-진입-구조)
2. [개별 실습 및 페이지 컴포넌트 (*.vue)](#2-개별-실습-및-페이지-컴포넌트-vue)
   - [2.1 Todolist 메인 페이지 (`ToDoList.vue`)](#21-todolist-메인-페이지-todolistvue)
   - [2.2 ToDo 카드 컴포넌트 (`ToDoCard.vue`)](#22-todo-카드-컴포넌트-todocardvue)
   - [2.3 ToDo 생성 페이지 (`CreateToDo.vue`)](#23-todo-생성-페이지-createtodovue)
   - [2.4 날짜 입력 커스텀 컴포넌트 (`NDateInput.vue`)](#24-날짜-입력-커스텀-컴포넌트-ndateinputvue)
   - [2.5 계정 로그인 / 회원가입 / 프로필 (`LoginAccount.vue`, `RegisterAccount.vue`, `ProfileAccount.vue`)](#25-계정-로그인--회원가입--프로필)
   - [2.6 Slot 메인 및 동적/범위 슬롯 실습 (`SlotContent.vue`, `ScopedSlot.vue`, `DynamicSlotName.vue` 등)](#26-slot-메인-및-동적범위-슬롯-실습)
   - [2.7 Axios GET 비동기 통신 (`AxiosGet.vue`)](#27-axios-get-비동기-통신-axiosgetvue)
3. [스크립트 및 설정 파일 (*.ts)](#3-스크립트-및-설정-파일-ts)
4. [TypeScript 및 Vuetify 3 UI 연동 정리](#4-typescript-및-vuetify-3-ui-연동-정리)
5. [💡 핵심 문법 정리 표 (Key Takeaways)](#5--핵심-문법-정리-표-key-takeaways)

---

## 1. 실행 환경 및 진입 구조

`todolist` 프로젝트는 **Vite + Vue 3 (Composition API) + Vuetify 3 + TypeScript + unplugin-vue-router** 기반으로 구현되었습니다.

### 1.1 엔트리 스크립트 (`main.ts`)
```typescript
import { createApp } from 'vue'
import App from './App.vue'
import { registerPlugins } from '@/plugins'

const app = createApp(App)
registerPlugins(app)
app.mount('#app')
```

### 1.2 루트 뷰 컴포넌트 (`App.vue`)
```vue
<template>
  <v-app>
    <v-main>
      <router-view />
    </v-main>
  </v-app>
</template>

<script lang="ts" setup>
  // 루트 컴포넌트
</script>
```

### 1.3 기본 레이아웃 (`layouts/default.vue`)
```vue
<template>
  <v-main>
    <router-view />
  </v-main>
</template>

<script lang="ts" setup>
  //
</script>
```

---

## 2. 개별 실습 및 페이지 컴포넌트 (*.vue)

### 2.1 Todolist 메인 페이지 (`ToDoList.vue`)
- **주요 기능**: 백엔드 REST API(`http://syntaxwiki.com:9993`)와 통신하여 유저 정보(`loadUser`)와 ToDo 카드 리스트(`refresh`)를 비동기로 조회 및 바인딩합니다.

```vue
<template>
<v-container class="pa-10">
    <v-row>
        <v-col>
            <div class="text-h5 font-weight-bold">
                <VAvatar color="grey" size="36">
                  <v-img cover :src="picture"></v-img>
                </VAvatar>
                To Do List
            </div>
            <div class="ma-2">안녕하세요. {{ username }} 님!</div>
        </v-col>
        <v-col class="text-right" align-self="center">
            <v-btn variant="elevated" class="text-none mr-5" @click="refresh">
                <v-icon>mdi-refresh</v-icon>
            </v-btn>
            <v-btn variant="elevated" color="red" class="text-none mr-5" to="/createtodo">
                <span class="mr-2">
                    <v-icon class="mr-2">mdi-plus</v-icon>
                    New Card
                </span>
            </v-btn>
        </v-col>
    </v-row>

    <v-row justify-start class="mt-7">
        <ToDoCard v-for="item in cards" :key="item.id" :item="item"></ToDoCard>
    </v-row>
</v-container>
</template>

<script setup>
    import { onMounted, ref } from 'vue'
    import axios from 'axios'
    import ToDoCard from './ToDoCard.vue';

    const cards = ref([{}])
    const username = ref('unknown')
    const picture = ref('https://cdn.vuetifyjs.com/images/profiles/marcus.jpg')
    const host = 'http://syntaxwiki.com:9993'

    const refresh = () => {
        axios.get(`${host}/items/?format=json`).then(function(response) {
            cards.value = response.data.results
        })
    }

    const loadUser = () => {
      axios.get(`${host}/auth/`, {}, { withCredentials: true })
      .then(function(res) {
        let user = res.data
        username.value = user.username
        picture.value = user.picture
      })
    }
    
    onMounted(() => {
        loadUser()
        refresh()
    })
</script>
```

---

### 2.2 ToDo 카드 컴포넌트 (`ToDoCard.vue`)
- **주요 기능**: `defineProps(['item'])`으로 상위 컴포넌트로부터 카드 데이터를 전달받아 Vuetify `<v-card>` 및 `<v-hover>` 범위 슬롯(Scoped Slot)으로 마우스 오버 시 상세 이동 오버레이(`<v-overlay>`)를 동적으로 표시합니다.

```vue
<template>
    <v-hover v-slot="{ isHovering, props }">
    <v-card 
        elevation="12"
        gradient="to bottom, rgba(0,0,0,.1), rgba(0,0,0,.5)"
        class="ma-4 card-item"
        width="100%"
        v-bind="props">
        <v-overlay
          :model-value="isHovering"
          class="align-center justify-center"
          scrim="#036358"
          contained>
            <v-btn variant="flat" :to="`item/${item.id}`">
                See more info
            </v-btn>
        </v-overlay>
        <v-card-title>{{ item.title }}</v-card-title>
        <v-card-subtitle>{{ item.created_at }} # {{ item.remote_ip }}</v-card-subtitle>
        <v-card-subtitle>DueTo {{ item.due_date }} D: {{ item.description }}</v-card-subtitle>
        <v-card-text>{{ item.content }}</v-card-text>
        <v-card-actions>
            <v-spacer></v-spacer>
            <v-btn size="small" color="surface-variant" variant="text" icon="mdi-heart"></v-btn>
            <v-btn size="small" color="surface-variant" variant="text" icon="mdi-bookmark"></v-btn>
            <v-btn size="small" color="surface-variant" variant="text" icon="mdi-share-variant"></v-btn>
        </v-card-actions>
    </v-card>
    </v-hover>
</template>

<script setup>
defineProps(['item'])
</script>
```

---

### 2.3 ToDo 생성 페이지 (`CreateToDo.vue`)
- **주요 기능**: 새 할 일 항목(제목, 내용, 설명, 마감일)을 입력받아 Axios HTTP POST 메서드로 서버에 신규 등록합니다. 커스텀 컴포넌트인 `NDateInput.vue`와 `v-model` 양방향 바인딩을 수행합니다.

```vue
<template>
    <v-container>
        <v-sheet width="400" class="mx-auto">
            <v-col>
                <div class="text-h5 font-weight-bold">To Do</div>
            </v-col>
            <v-form>
                <v-text-field v-model="title" prepend-icon="mdi-format-title" label="title"></v-text-field>
                <v-textarea v-model="content" label="content" prepend-icon="mdi-content-paste" clearable></v-textarea>
                <v-text-field v-model="description" prepend-icon="mdi-subtitles" label="description"></v-text-field>
                <n-date-input v-model="dueDate" prepend-icon="mdi-clipboard-text-clock" label="due date"></n-date-input>
                <v-row>
                    <v-spacer></v-spacer>
                    <v-btn variant="elevated" color="blue" class="text-none ma-2" @click="submitForm" prepend-icon="mdi-plus">저장</v-btn>
                    <v-btn variant="elevated" class="text-none ma-2" to="/" prepend-icon="mdi-close">닫기</v-btn>
                </v-row>
            </v-form>
        </v-sheet>
    </v-container>
</template>

<script setup>
    import { ref } from 'vue'
    import axios from 'axios'
    import NDateInput from '@/components/NDateInput.vue'

    const title = ref('')
    const content = ref('')
    const description = ref('')
    const dueDate = ref(new Date())

    const submitForm = () => {
        axios.post('http://syntaxwiki.com:9993/items/', {
            title: title.value,
            content: content.value,
            description: description.value,
            due_date: dueDate.value
        }).then((res) => {
            alert('등록되었습니다.')
        }).catch((error) => {
            alert(error)
        })
    }
</script>
```

---

### 2.4 날짜 입력 커스텀 컴포넌트 (`NDateInput.vue`)
- **주요 기능**: `<v-text-field>`와 `<v-dialog>`, `<v-date-picker>`를 조합하여 날짜 선택 대화상자를 구현하고 `v-model` 이벤트 발신(`update:modelValue`)을 처리합니다.

```vue
<template>
    <v-dialog width="auto">
        <template v-slot:activator="{ props: activatorProps }">
            <v-text-field
              v-bind="activatorProps"
              :model-value="displayFormatted"
              :label="label"
              :prepend-icon="prependIcon"
              readonly>
            </v-text-field>
        </template>
        <template v-slot:default="{ isActive }">
            <v-card>
                <v-date-picker v-model="internalDate" @update:model-value="isActive.value = false"></v-date-picker>
            </v-card>
        </template>
    </v-dialog>
</template>

<script setup>
import { ref, computed } from 'vue'

const props = defineProps({
    modelValue: { type: Date, default: () => new Date() },
    label: String,
    prependIcon: String
})

const emit = defineEmits(['update:modelValue'])

const internalDate = computed({
    get: () => props.modelValue,
    set: (val) => emit('update:modelValue', val)
})

const displayFormatted = computed(() => {
    return props.modelValue ? props.modelValue.toLocaleDateString() : ''
})
</script>
```

---

### 2.5 계정 로그인 / 회원가입 / 프로필 (`LoginAccount.vue`, `RegisterAccount.vue`, `ProfileAccount.vue`)
- **로그인 (`LoginAccount.vue`)**: 사용자 이메일/비밀번호 입력 및 Vuetify 유효성 검사 규칙(`rules`) 적용, `axios.post` 기반 세션 인증.
- **회원가입 (`RegisterAccount.vue`)**: 사용자명, 이메일, 비밀번호, 약관 동의 체크박스 및 i18n 국제화 다국어 적용.
- **프로필 (`ProfileAccount.vue`)**: 탭 메뉴(`<v-tabs>`), 프로필 아바타, 세부 계정 설정 UI 구현.

---

### 2.6 Slot 메인 및 동적/범위 슬롯 실습
- **`SlotContent.vue`**: 슬롯 컨텐츠 전달 테스트.
- **`ScopedSlot.vue` / `ScopedSlotComponent.vue`**: 자식 컴포넌트의 내부 데이터를 부모 컴포넌트로 전달하는 범주 슬롯(`v-slot="{ text, count }"`) 실습.
- **`DynamicSlotName.vue`**: `v-slot:[slotName]` 동적 슬롯 바인딩 및 이름 지정 슬롯(`v-slot:header`, `v-slot:footer`) 활용.

---

### 2.7 Axios GET 비동기 통신 (`AxiosGet.vue`)
- **`AxiosGet.vue`**: `axios.get()`을 사용하여 외부 API 데이터를 요청하고 `response.data` 결과를 reactive 상태에 저장하여 화면에 바인딩하는 기초 예제.

---

## 3. 스크립트 및 설정 파일 (*.ts)

### 3.1 플러그인 레지스트리 (`plugins/index.ts`)
```typescript
import vuetify from './vuetify'
import router from '../router'
import i18n from './i18n'
import type { App } from 'vue'

export function registerPlugins (app: App) {
  app
    .use(vuetify)
    .use(router)
    .use(i18n)
}
```

### 3.2 i18n 국제화 설정 (`plugins/i18n.ts`)
```typescript
import { createI18n } from 'vue-i18n'

export default createI18n({
  legacy: false,
  locale: 'ko',
  fallbackLocale: 'en',
  messages: {
    en: { welcome: 'Welcome' },
    ko: { welcome: '환영합니다' }
  }
})
```

---

## 4. TypeScript 및 Vuetify 3 UI 연동 정리

- **Vuetify 3 주요 컴포넌트 활용**:
  - `<v-card>`, `<v-card-title>`, `<v-card-subtitle>`, `<v-card-text>`, `<v-card-actions>`: 카드 기반 리스트 레이아웃
  - `<v-hover>` & `<v-overlay>`: 마우스 호버 상태에 반응하는 인터랙티브 레이어 제어
  - `<v-dialog>` & `<v-date-picker>`: 모달 형태의 커스텀 날짜 피커 팝업 생성
  - `<v-text-field>`, `<v-textarea>`, `<v-checkbox>`, `<v-form>`: Vue 3 폼 바인딩 및 검증 규칙 연동
- **TypeScript 타입 안전성**:
  - `App` 객체의 타입 정의 (`import type { App } from 'vue'`)
  - Vue Router 컴포넌트 자동 타입 생성 (`typed-router.d.ts`, `components.d.ts`)

---

## 5. 💡 핵심 문법 정리 표 (Key Takeaways)

| 개념 | 주요 기능 및 문법 예시 | 핵심 특징 |
|------|------------------------|-----------|
| **Composition API** | `ref()`, `computed()`, `onMounted()` | 반응성 상태 정의 및 생명주기 훅 관리 |
| **Vuetify Scoped Slot** | `<v-hover v-slot="{ isHovering, props }">` | 자식 UI 컴포넌트의 가시성/이벤트 상태를 부모 슬롯으로 바인딩 |
| **Custom Date Picker** | `v-dialog` + `v-date-picker` (`NDateInput.vue`) | 텍스트 필드 클릭 시 날짜 선택 모달 노출 및 `update:modelValue` 발신 |
| **Axios Async HTTP** | `axios.get()`, `axios.post()` | RESTful API 서버와 JSON 데이터 송수신 및 세션 처리(`withCredentials`) |
| **File-based Router** | `unplugin-vue-router` (`item.[itemid].vue`) | 파일 이름 규칙 기반 자동 URL 경로 매핑 및 파라미터 전달 |
| **Vue i18n** | `createI18n()`, `t('key')` | 다국어(한국어, 영어 등) 언어팩 스위칭 처리 |

